# 🎟️ راهنمای سیستم کدهای تخفیف

> مدیریت کامل از داخل ربات، بدون نیاز به دستکاری دیتابیس یا کد.

## 📍 مسیر دسترسی (ادمین)

```
منوی ادمین ← 🏷️ تعرفه‌ها و قیمت‌گذاری ← 🎟️ کدهای تخفیف
```

در همین صفحه، آمار لحظه‌ای (تعداد کدها، دفعات استفاده، مجموع تخفیف اعطاشده) نمایش داده می‌شود.

---

## ➕ ساخت کد جدید (ویزارد ۱۰ مرحله‌ای)

| مرحله | سوال | توضیح |
|------|------|-------|
| ۱ | 🔑 کد | ۳ تا ۳۲ کاراکتر (حروف/عدد/`-`/`_`) — یا دکمه **🎲 تولید خودکار** |
| ۲ | 💸 نوع | **٪ درصدی** یا **💰 مبلغی (تومان)** |
| ۳ | 🔢 مقدار | درصد ۱ تا ۱۰۰، یا مبلغ به تومان — **معادل قیمت روی همه پلن‌ها خودکار محاسبه و نمایش داده می‌شود** |
| ۴ | 🎯 سقف تخفیف | فقط برای درصدی؛ مثلاً «۲۰٪ تا سقف ۲۰۰ هزار تومان» (قابل رد شدن) |
| ۵ | 🌍/👤 دامنه | **عمومی** (همه) یا **شخصی** (فقط آیدی‌هایی که می‌دهی؛ بقیه خطای «این کد برای شما نیست» می‌گیرند) |
| ۶ | 📦 پلن‌های مشمول | چندانتخابی؛ اگر هیچ‌کدام را نزنی = همه پلن‌ها |
| ۷ | 🔢 سقف کل | ظرفیت کد (مثلاً ۱۰۰ نفر اول) یا نامحدود |
| ۸ | 👤 سقف هر کاربر | هر نفر چند بار (پیش‌فرض امن: ۱ بار) |
| ۹ | ⏰ اعتبار | عدد = روز از الان (ارقام فارسی هم قبول است) / تاریخ شمسی `1405/07/15` / میلادی `2026-10-07` / نامحدود |
| ۱۰ | 🧾 کف خرید، 🛒 فقط-خرید-اول، 📝 عنوان | اختیاری، بعد تأیید نهایی با پیش‌فاکتور کامل |

### 💡 مثال‌های آماده

- **کمپین عمومی:** `SUMMER20` — ۲۰٪، عمومی، ظرفیت ۲۰۰، هر کاربر ۱ بار، ۳۰ روزه
- **هدیه شخصی:** `VIP-50000` — ۵۰ هزار تومان، شخصی برای ۲ آیدی، نامحدود تا ۷ روز
- **رایگان کامل:** درصد ۱۰۰٪ — کاربر **بدون پرداخت بانکی**، با دکمه «🎁 دریافت رایگان» فعال می‌شود
- **پلن خاص:** کد فقط برای پلن «یک‌ساله» + کف خرید ۵۰۰ هزار تومان + فقط خرید اول

---

## 🛠️ امکانات هر کد (صفحه جزئیات)

- 🔄 فعال/غیرفعال کردن لحظه‌ای (بدون حذف)
- ✏️ ویرایش همه فیلدها: مقدار، سقف، کف خرید، ظرفیت‌ها، انقضا، عنوان، دامنه، فقط-خرید-اول
  (متن کد برای سلامت حسابرسی غیرقابل تغییر است؛ برای کد جدید، یکی بساز و قبلی را غیرفعال کن)
- 👥 مدیریت کاربران مجاز کد شخصی (افزودن/حذف تکی + شمارش مصرف هر نفر)
- 📦 تغییر پلن‌های مشمول
- 📜 تاریخچه استفاده (کی، کدام پلن، قیمت اصلی ← تخفیف ← مبلغ پرداختی)
- 📊 آمار (دفعات، کاربران یکتا، مجموع تخفیف، مجموع دریافتی، آخرین استفاده)
- 📣 اطلاع‌رسانی: کد شخصی → فقط به کاربران مجاز | کد عمومی → به همه کاربران (با تأیید قبلی)
- 🗑️ حذف امن: کد حذف می‌شود ولی **تاریخچه استفاده‌ها برای گزارش‌ها حفظ می‌شود**

### 🔍 فیلترهای لیست

`📋 همه` `🟢 فعال` `🌍 عمومی` `👤 شخصی` `🔴 منقضی` `⛔ تمام‌شده` + صفحه‌بندی

---

## 🛒 تجربه کاربر

1. تعرفه‌ها ← انتخاب پلن ← **🧾 پیش‌فاکتور** (قیمت اصلی مشخص است)
2. دکمه **🎟️ اعمال کد تخفیف** ← وارد کردن کد ← نمایش مبلغ تخفیف و **مبلغ نهایی**
3. اگر نهایی صفر شد ← **🎁 دریافت رایگان** (بدون درگاه)؛ وگرنه پرداخت کیف‌پول بیل با مبلغ تخفیف‌خورده
4. کد اشتباه/منقضی/تمام‌شده/شخصیِ دیگران → پیام خطای دقیق فارسی + امکان تلاش مجدد یا ادامه بدون تخفیف
5. بخش **🎟️ کدهای تخفیف من** در تعرفه‌ها: کدهای شخصی فعال هر کاربر (فقط مال خودش را می‌بیند)

---

## 🛡️ نکات حرفه‌ای پیاده‌سازی‌شده

- **اعتبارسنجی دو مرحله‌ای:** هم لحظه اعمال کد، هم لحظه پرداخت/دریافت (جلوگیری از سوءاستفاده از فاصله زمانی)
- **ثبت اتمیک** مصرف (`BEGIN IMMEDIATE`) — در استفاده هم‌زمان، ظرفیت اضافه برداشت نمی‌شود
- اگر پول گرفته شد ولی کد بین فاکتور و پرداخت مشکل پیدا کرد (اتمام/حذف)، **دسترسی کاربر حتماً فعال می‌شود** و مصرف با پرچم ثبت + به ادمین گزارش می‌شود
- کدها **case-insensitive** هستند (`vip-50` = `VIP-50`)، ارقام فارسی پذیرفته می‌شود، فاصله‌ها نادیده گرفته می‌شوند
- مبلغ نهایی هرگز منفی نمی‌شود (تخفیفِ بیشتر از قیمت = رایگان)
- هر پرداخت، کد/مبلغ اصلی/مبلغ تخفیف را در تاریخچه پرداخت کاربر ذخیره می‌کند (قابل مشاهده در پروفایل کاربر)
- همه رویدادها (ساخت، ویرایش، اطلاع‌رسانی، اعمال، خرید) در **گزارش فعالیت** ثبت می‌شود
- سازگاری کامل با فاکتورهای قدیمی (payload بدون تخفیف) — هیچ خرید قبلی‌ای خراب نمی‌شود

---

## ⚠️ چند توصیه مدیریتی

- برای کدهای عمومیِ پرمخاطب حتماً **ظرفیت کل + سقف ۱ بار هر کاربر** بگذار.
- کد ۱۰۰٪ را **شخصی + ظرفیت‌دار + انقضادار** صادر کن تا پخش نشود.
- قبل از پخش عمومی، با یک کاربر تستی یک‌بار مسیر خرید را امتحان کن.
- برای جشنواره‌ها از **سقف تخفیف درصدی** استفاده کن تا روی پلن‌های گران، تخفیف کنترل‌شده بماند.
